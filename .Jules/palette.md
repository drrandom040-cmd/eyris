## 2025-05-14 - Search Input UX Pattern
**Learning:** For search-intensive input fields, combining `ImeAction.Next` or `ImeAction.Search` with trailing "clear" buttons significantly improves the speed and ease of multi-field search operations.
**Action:** Use `trailingIcon` for clear buttons and appropriate `KeyboardOptions`/`KeyboardActions` for all multi-field search screens.
